math_print:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: student
  status: <%= Print::STATUS[:pending_payment] %>
  revoked: false

physics_print:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: teacher
  status: <%= Print::STATUS[:pending_payment] %>
  revoked: false

os_print:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: teacher
  status: <%= Print::STATUS[:paid] %>
  revoked: false

second_math_print:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: second_operator
  customer: student
  status: <%= Print::STATUS[:pending_payment] %>
  revoked: false

second_physics_print:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: second_operator
  customer: teacher
  status: <%= Print::STATUS[:pending_payment] %>
  revoked: false

scheduled_math_print:
  printer: ''
  user: operator
  customer: student
  status: <%= Print::STATUS[:paid] %>
  scheduled_at: <%= 1.hour.from_now.to_s(:db) %>
  revoked: false
  
math_print_with_credit:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: student
  status: <%= Print::STATUS[:paid] %>
  revoked: false
  
math_print_to_pay_later_1:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: student
  status: <%= Print::STATUS[:pay_later] %>
  revoked: false

math_print_to_pay_later_2:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: student
  status: <%= Print::STATUS[:pay_later] %>
  revoked: false
  created_at: <%= Time.now.advance(months: -2, days: -1).to_s(:db) %>

physics_print_to_pay_later:
  printer: <%= Cups.show_destinations.detect { |p| p =~ /pdf/i } %>
  user: operator
  customer: teacher
  status: <%= Print::STATUS[:pay_later] %>
  revoked: false