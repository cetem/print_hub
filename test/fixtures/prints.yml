math_print:
  printer: <%= Cups.show_destinations.select {|p| p =~ /pdf/i}.first %>
  user: operator
  customer: student

physics_print:
  printer: <%= Cups.show_destinations.select {|p| p =~ /pdf/i}.first %>
  user: operator
  customer: teacher

os_print:
  printer: <%= Cups.show_destinations.select {|p| p =~ /pdf/i}.first %>
  user: operator
  customer: teacher

second_math_print:
  printer: <%= Cups.show_destinations.select {|p| p =~ /pdf/i}.first %>
  user: second_operator
  customer: student

second_physics_print:
  printer: <%= Cups.show_destinations.select {|p| p =~ /pdf/i}.first %>
  user: second_operator
  customer: teacher