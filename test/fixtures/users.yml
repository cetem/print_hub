operator:
  name: Operator
  last_name: Operator
  language: <%= LANGUAGES.first.to_s %>
  email: operator@printhub.com
  default_printer: <%= Cups.show_destinations.select {|p| p =~ /pdf/i}.first %>
  lines_per_page: 10
  username: operator
  password_salt: <%= salt = hex_token %>
  crypted_password: <%= encrypt('operator123', salt) %>
  admin: true
  enable: true
  not_shifted: false
  avatar_file_name: avatar.gif
