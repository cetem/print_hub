<ul>
  <% docs.each do |doc| %>
    <li id="auto_doc_<%= doc.id %>">
      <%= highlight doc.name, params[:q] %>
      <span class="informal">
        <%= highlight doc.tags.map(&:to_s).join(' ## '), params[:q] %>
      </span>
    </li>
  <% end %>
</ul>