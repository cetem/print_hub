<% message = link_to(
  '-',
  remove_from_order_catalog_path(@document),
  :title => t('view.catalog.remove_from_order.title'),
  :remote => true, :method => :delete, :class => 'remove_link remove_from_order'
) %>
<% id = "#document_#{@document.id}_to_order" %>
$('<%= id %>').html('<%= message %>');